<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/bg_challenge_card"
    android:padding="16dp"
    android:layout_marginBottom="10dp"
    android:gravity="center_vertical">

    <!-- Icono del desafío -->
    <FrameLayout
        android:layout_width="48dp"
        android:layout_height="48dp">

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_icon_circle" />

        <ImageView
            android:id="@+id/ivChallengeIcon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            tools:src="@drawable/ic_star"
            android:contentDescription="@null" />
    </FrameLayout>

    <!-- Info del desafío -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/tvChallengeTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/hub_text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="Primer Click" />

        <TextView
            android:id="@+id/tvChallengeDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/hub_text_hint"
            android:textSize="12sp"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="Juega tu primera partida de Kaisen Clicker" />

        <!-- Barra de progreso -->
        <ProgressBar
            android:id="@+id/progressChallenge"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginTop="8dp"
            android:max="100"
            android:progress="50"
            android:progressTint="@color/hub_accent"
            android:progressBackgroundTint="@color/hub_surface_light" />

        <TextView
            android:id="@+id/tvChallengeProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/hub_text_hint"
            android:textSize="11sp"
            tools:text="3/5" />
    </LinearLayout>

    <!-- Badge de estado -->
    <TextView
        android:id="@+id/tvChallengeStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:background="@drawable/bg_challenge_locked"
        tools:text="+50 pts" />

</LinearLayout>

